const getNewArray = function(arr) {
  const newArr = arr.filter(str => str.length >= 5 && str.includes('a'));

  return newArr;
}

const strings = ["Sample", "Hub", "cherry", "grape", "kiwi", "Silak"];
const newArray = getNewArray(strings);
console.log(newArray); 